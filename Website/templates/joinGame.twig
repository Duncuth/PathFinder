<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Second Player - PathFinder</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.0/font/bootstrap-icons.min.css" rel="stylesheet">
</head>
<body class="vh-100" style="background-color: #f0f0e9;">

<!-- Bouton retour -->
<a href="{{ basePath }}/gamemode" class="btn btn-outline-dark btn-lg px-3 py-2 position-absolute top-0 start-0 m-3">
  <i class="bi bi-arrow-left"></i> Retour
</a>

    <!-- Contenu principal -->
    <div class="container vh-100 d-flex flex-column justify-content-center align-items-center text-center">

        <!-- Titre -->
        <h2 class="mb-3 fw-bold" style="font-size: 3.2rem;">Deuxième joueur</h2>
        <p class="mb-4 mt-2 text-muted" style="font-size: 1.3rem;">Jouez en tant qu'invité ou connectez-vous pour sauvegarder votre progression.</p>

        <!-- Formulaire d'entrée du pseudo -->
        <form method="POST" action="{{ basePath }}/secondPlayer" class="mb-4 mt-2 w-50">
            <input type="text" name="username" class="form-control form-control-lg text-center" id="username"
                   placeholder="Entrez votre pseudo (invité)" aria-label="Username Input">
            <div class="d-flex mt-4 gap-4 justify-content-center">
                <button type="submit" class="btn btn-secondary btn-lg px-5 py-3">
                    Jouer en tant qu'invité
                </button>
                <a href="{{ basePath }}/login" class="btn btn-primary btn-lg px-5 py-3">
                    Se connecter
                </a>
            </div>
        </form>

    </div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
