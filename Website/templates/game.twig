<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Graphes - PathFinder</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.0/font/bootstrap-icons.min.css" rel="stylesheet">
</head>
<body class="vh-100" style="background-color: #f0f0e9;">

<!-- Bouton retour -->
<a href="/joinGame" class="btn btn-outline-dark btn-lg px-3 py-2 position-absolute top-0 start-0 m-3">
    <i class="bi bi-arrow-left"></i> Retour
</a>

<!-- Contenu principal -->
<div class="container vh-100 d-flex flex-column justify-content-center align-items-center text-center">

    <!-- Titre principal -->
    <h2 class="mb-3 fw-bold" style="font-size: 3.2rem;">Visualisation des Graphes</h2>
    <p class="mb-4 mt-2 text-muted" style="font-size: 1.3rem;">
        Parcourez et analysez les graphes disponibles dans PathFinder.
    </p>

    <!-- Liste des graphes -->
    <div class="w-75 text-start">
        {% if data|length > 0 %}
            <div class="list-group">
                {% for graph in data %}
                    <div class="list-group-item d-flex justify-content-between align-items-center">
                        <div>
                            <h5 class="mb-1">{{ graph.name|e }}</h5>
                            <p class="mb-0 text-muted">
                                Sommets : {{ graph.vertex_count }} | Arcs : {{ graph.edge_count }} | Statut : {{ graph.status|capitalize }}
                            </p>
                        </div>
                        <div>
                            <!-- Bouton pour jouer -->
                            <a href="/graph/play/{{ graph.id }}" class="btn btn-primary btn-sm me-2">
                                <i class="bi bi-controller"></i> Jouer
                            </a>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <p class="text-muted">Aucun graphe disponible pour le moment.</p>
        {% endif %}
    </div>

    <!-- Bouton d'ajout de graphe -->
    <div class="mt-5">
        <a href="/add-graph" class="btn btn-success btn-lg px-5 py-3">
            Ajouter un nouveau graphe
        </a>
    </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
