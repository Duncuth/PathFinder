<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestion des graphes acceptés - Administrateur</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Assure-toi que Bootstrap Icons est bien inclus -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.0/font/bootstrap-icons.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-white">
<div class="container vh-100 d-flex justify-content-center align-items-center">
    <div class="card p-5 shadow-lg bg-light text-dark" style="max-width: 600px; width: 100%;">

        <!-- Bouton retour -->
        <div class="d-flex justify-content-start mb-3">
            <a href="{{ path('page_precedente') }}" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left"></i> Retour
            </a>
        </div>

        <h3 class="mb-4 text-center">Gestion des graphes acceptés</h3>

        <!-- Liste des graphes acceptés -->
        <ul class="list-group">
            {% for graph in graphs %}
            <li class="list-group-item d-flex justify-content-between align-items-center">
                <!-- Graph info -->
                <span>{{ graph.name }}</span>
                <!-- Action buttons -->
                <span>
                    <button class="btn btn-outline-secondary btn-sm">
                        <i class="bi bi-eye"></i>
                    </button>
                    <button class="btn btn-outline-danger btn-sm">
                        <i class="bi bi-x"></i>
                    </button>
                </span>
            </li>
            {% endfor %}
        </ul>
    </div>
</div>

<!-- Optional Bootstrap Icons CDN -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.0/font/bootstrap-icons.min.js"></script>
</body>
</html>