<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gestion des Graphes - PathFinder</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.0/font/bootstrap-icons.min.css" rel="stylesheet">
</head>
<body class="vh-100" style="background-color: #f0f0e9;">

  <!-- Bouton de retour -->
  <a href="{{ basePath }}" class="btn btn-outline-dark btn-lg px-3 py-2 position-absolute top-0 start-0 m-3">
    <i class="bi bi-arrow-left"></i> Retour
  </a>

  <!-- Contenu principal -->
  <div class="container vh-100" style="padding-top: 4.5rem;">
    <h2 class="mb-4 text-center  fs-1 text-uppercase">Gestion des Graphes en attente</h2>

        <!-- Tableau des graphes -->
        <div class="d-flex d-flex justify-content-center pt-1">
            <table class="table table-striped border border-black w-75" style="max-height: 33rem; overflow-y: auto;">
                <thead class="table-dark">
                    <tr>
                        <th class="ps-4">#</th>
                        <th class="ps-4">Nom</th>
                        <th class="ps-4">Visuel</th>
                        <th class="w-25">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td class="ps-4 fw-bold align-middle">1</td>
                        <td class="ps-4 align-middle">Graphe1</td>
                        <td class="ps-4" style="position: relative; height: 32px;">
                            <button class="btn btn-outline btn-sm" style="position: absolute; top: 50%; left: 14%; transform: translate(-50%, -50%);">
                                <i class="bi bi-eye-fill" style="font-size: 1.5rem;"></i>
                            </button>
                        </td>
                        <td>
                            <div class="d-flex gap-3" style="height: 32px;">
                                <button class="btn btn-success btn-sm">
                                    <i class="bi bi-check-circle"></i> Accepter
                                </button>
                                <button class="btn btn-danger btn-sm">
                                    <i class="bi bi-x-circle"></i> Refuser
                                </button>
                            </div>
                        </td>
                    </tr>
                    <!-- Ajouter plus de lignes ici -->
                </tbody>
            </table>
        </div>
  </div>

</body>
</html>
